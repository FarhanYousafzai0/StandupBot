# `components/`

Shared UI: layout chrome, feature blocks, and `ui/` for shadcn primitives once installed.

Use **DESIGN.md** tokens via Tailwind (`bg-parchment`, `text-olive-gray`, `font-display`, etc.) — see `app/globals.css` `@theme`.
